# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=events
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Generator
EVENT_RATE=50          # 초당 생성 이벤트 수
EVENT_DURATION=60      # 실행 시간 (초), 0이면 무한 실행
EVENT_BATCH_SIZE=100   # bulk insert 단위
